import React, { Component } from 'react';

export default class AddMoviePage extends Component {
    render() {
        return (
            <div>
                PLS
            </div>
        );
    }
}
